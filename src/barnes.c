#include <stdio.h>

#include "quad.h"

void get_center_of_mass(b_node *point)
{
    for(int i = 0; i < point->child_count; i++)
    {
        if (point->children[i] == NULL)
        {

        }
    }
}
